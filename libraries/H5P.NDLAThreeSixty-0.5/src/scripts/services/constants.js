/** @constant {number} MAX_PITCH Maximum pitch. */
export const MAX_PITCH = Math.PI / 2;

/** @constant {number} CAMERA_NEAR Camera near value. */
export const CAMERA_NEAR = 0.1;

/** @constant {number} CAMERA_FAR Camera far value. */
export const CAMERA_FAR = 1000;

/** @constant {number} FOV_PANORAMA Field of View for panoramas. */
export const FOV_PANORAMA = 53;

/** @constant {number} FOV_SPHERE Field of View for spheres. */
export const FOV_SPHERE = 75;

/** @constant {number} DEFAULT_Friction Default friction. */
export const DEFAULT_FRICTION = 400;

/** @constant {number} DEFAULT_WORLD_RADIUS Default world radius. */
export const DEFAULT_WORLD_RADIUS = 10;

/** @constant {string} DEFAULT_ROTATION_ORDER Default rotation order. */
export const DEFAULT_ROTATION_ORDER = 'YXZ';

/** @constant {string} DEFAULT_ROATIO Default aspect ratio. */
export const DEFAULT_RATIO = 16 / 9;

/** @constant {number} ZOOM_MIN Minimum zoom value. */
export const ZOOM_MIN = 1;

/** @constant {number} ZOOM_MAX Maximum zoom value. */
export const ZOOM_MAX = 15;

/** @constant {number} ZOOM_SPEED Zoom speed */
export const ZOOM_SPEED = 2.5;

/** @constant {number} ZOOM_SPEED_TOUCH Zoom speed for touch */
export const ZOOM_SPEED_TOUCH = 1.3;